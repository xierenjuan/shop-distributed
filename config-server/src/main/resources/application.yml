spring:
  cloud:
    config:
      server:
        git:
          uri: git@github.com:xierenjuan/spring-cloud.git
          search-paths: configdir # 搜索路径
          username: xierenjun
          password: 111111
  application:
    name: config-server
  profiles:
    active: @profile.env@
server:
  port: 6064
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:6060/eureka/
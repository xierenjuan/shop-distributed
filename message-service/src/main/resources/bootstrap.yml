spring:
    cloud:
        config:
            label: master
            uri: http://localhost:6064
            name: application-message-@profile.env@,default # 文件名
            profile: @profile.env@